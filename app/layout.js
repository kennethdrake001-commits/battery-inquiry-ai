import "./globals.css";

export const metadata = {
  title: "储能电池询盘成交 AI 助手",
  description: "Energy storage battery export inquiry AI assistant"
};

export default function RootLayout({ children }) {
  return (
    <html lang="zh-CN">
      <body>{children}</body>
    </html>
  );
}
